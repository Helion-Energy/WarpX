# base input parameters
FILE = inputs_test_rz_theta_implicit_mhd_vacuum_resistive_pc

# Assembly-validation twin of the preconditioned stiff RZ vacuum-resistive
# deck: at every active preconditioner update the sparse rows the direct
# resistive solver would factor are assembled from the shared stencil
# emission and checked to roundoff against the matrix-free operator on a
# deterministic pseudo-random vector (the run aborts on any mismatch).
# Two ranks exercise the owner-mask deduplication of shared box-interface
# faces, the periodic z-nodal identification, and the gathered global
# pattern. The physics assertions are the baseline's, unchanged.
pc_mhd_block.resistive_validate_assembly = 1
