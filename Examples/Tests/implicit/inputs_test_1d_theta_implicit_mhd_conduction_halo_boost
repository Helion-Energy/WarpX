# base input parameters
FILE = inputs_base_1d_theta_implicit_mhd_conduction_halo_boost

# Quantitative arm: the reference code's dp_mn = 1 multiplicative ion halo boost
# keyed on the STATIC reference base (the reference code's en00 card) at
# n_ref = 10 n0, so the ion chi_perp is MULTIPLIED by exactly
# dp (n_ref/n0)^2 = 100 (measured against the kernel's first-principles
# raw value); the electron channel is frozen on this window (no
# electron boost). ~50% ion ripple decay over 10 steps.
implicit_mhd.conduction_halo_boost = 1.0
implicit_mhd.vacuum_reference_base_density = 10.0 * n0 * m_p
warpx.const_dt = 4.3e-9
max_step = 10
