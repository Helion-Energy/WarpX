#################################
############ CONSTANTS ##########
#################################

# Reference-parity shaped-wall NO-SLIP PIN (implicit_mhd.wall_no_slip): the
# same sheared column the wall-viscosity-band test uses -- uniform
# density and pressure, a linear axial shear u_z = A r inside a cylinder
# wall (r_w = 0.30 m, cells i >= 15 masked at dr = 0.02), B = 0, J = 0,
# eta = 0, no conduction, no Joule -- but with the viscosity live
# EVERYWHERE (no wall_viscosity_mask), so the wall-adjacent rows carry
# real viscous stress and the pin has something to hold against.
#
# The reference code omits the vr/vz rows of the wall-contour vertices AND of the
# adjacent cut-cell "skin" layer from its implicit momentum matrices
# (vp.f90:221/233 bnd = 'i', sten.f:115-121, solv.f:88-96), and its
# velocities are allocated at exactly zero (init_cond.f90:82):
# those two vertex layers are pinned at u = 0 for the whole run
# (measured bit-exact zero on all 762 wall and 762 skin vertices of
# The reference shot's earlier dump while the interior ran 1.7e5-6.2e5 m/s).
# wall_no_slip_width = 2 is that wall + skin depth.
#
# Contract under test (see analysis_mhd_wall_no_slip.py), against the
# wall_no_slip = 0 twin of the identical deck:
#  - pin ON: the first 2 LIVE cell rows (i = 13, 14) carry EXACTLY zero
#    momentum in all three components, for the whole run;
#  - pin OFF: those same rows slide essentially freely (our stair-face
#    image copies the tangential momentum verbatim -- a perfect-slip
#    wall), staying within a few percent of the initial rho A r;
#  - the pin deposits wall drag into the first UNPINNED row (i = 12),
#    which is the transport channel the free-slip wall does not have;
#  - the far interior (i <= 8) is unchanged from the OFF twin to solver
#    precision: the pin must not reach past its own shear layer.
# The run is kept SHORT (nu t / dr^2 = 7.5e-3) so the shear layer stays
# inside the band-adjacent rows.
my_constants.n0 = 1.0e20
my_constants.rho0 = n0 * m_p
my_constants.T0_ev = 4.0
my_constants.A = 1.0e3
my_constants.nu = 20.0
my_constants.gamma = 5.0/3.0
my_constants.B0 = 0.01
my_constants.Lr = 0.48
my_constants.Lz = 0.32
my_constants.nr = 24
my_constants.nz = 8
my_constants.dt = 5.0e-8

#################################
####### GENERAL PARAMETERS ######
#################################

max_step = 3
amr.n_cell = nr nz
amr.max_grid_size = 8
amr.max_level = 0

geometry.dims = RZ
warpx.n_rz_azimuthal_modes = 1
geometry.prob_lo = 0.0 -0.5*Lz
geometry.prob_hi = Lr 0.5*Lz
boundary.field_lo = none none
boundary.field_hi = pec none

#################################
############ NUMERICS ###########
#################################

warpx.abort_on_warning_threshold = high
warpx.verbose = 1
warpx.const_dt = dt
algo.maxwell_solver = hybrid
algo.current_deposition = direct
algo.evolve_scheme = theta_implicit_mhd

implicit_evolve.nonlinear_solver = newton
implicit_evolve.theta = 1.0

newton.verbose = true
newton.absolute_tolerance = 1.0e-12
newton.relative_tolerance = 1.0e-10
newton.max_iterations = 12
newton.diagnostic_file = diags/newton.txt

gmres.verbose_int = 0
gmres.relative_tolerance = 1.0e-10
gmres.max_iterations = 300

#################################
######### HYBRID OHM LAW ########
#################################

hybrid_pic_model.elec_temp = T0_ev
hybrid_pic_model.n0_ref = n0
hybrid_pic_model.gamma = gamma
hybrid_pic_model.n_floor = 1.0e10
hybrid_pic_model.plasma_resistivity(rho,J,t) = 0.0
hybrid_pic_model.include_hall_term = false
hybrid_pic_model.include_electron_pressure_term = false

#################################
######## IMPLICIT MHD STATE #####
#################################

implicit_mhd.reference_mass_density = rho0
implicit_mhd.reference_magnetic_field = B0
implicit_mhd.gamma_e = gamma
implicit_mhd.gamma_i = gamma
implicit_mhd.mass_density_floor = 1.0e-6 * rho0
implicit_mhd.electron_pressure_floor = 1.0e-6 * n0 * T0_ev * q_e
implicit_mhd.ion_pressure_floor = 1.0e-6 * n0 * T0_ev * q_e
implicit_mhd.ion_closure = total_energy
implicit_mhd.fluid_flux = central
implicit_mhd.viscosity = nu
implicit_mhd.include_joule_heating = false
implicit_mhd.mass_density(x,y,z) = rho0
implicit_mhd.electron_pressure(x,y,z) = n0 * T0_ev * q_e
implicit_mhd.ion_pressure(x,y,z) = n0 * T0_ev * q_e
implicit_mhd.velocity_z(x,y,z) = A * x

# Cylinder conducting wall at r = 0.30 m (constant continuation covers
# the z ghosts); electromagnetic-only wall (no thermal BC): the no-slip
# pin is the only wall-fluid momentum coupling under test, and it is
# deliberately independent of wall_thermal_bc.
implicit_mhd.wall_model = pec
implicit_mhd.wall_polyline_file = wall_cylinder_r30.csv
implicit_mhd.wall_no_slip = 1
implicit_mhd.wall_no_slip_width = 2

#################################
########### DIAGNOSTICS #########
#################################

diagnostics.diags_names = diag1
diag1.intervals = 3
diag1.diag_type = Full
diag1.format = plotfile
diag1.file_prefix = diags/diag
diag1.fields_to_plot = implicit_mhd_mass_density \
                       implicit_mhd_ion_energy \
                       implicit_mhd_electron_energy \
                       implicit_mhd_momentum_r implicit_mhd_momentum_t \
                       implicit_mhd_momentum_z
diag1.write_species = 0
