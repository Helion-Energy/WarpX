# base input parameters
FILE = inputs_test_rz_theta_implicit_mhd_resistive_pc_assembly

# Matrix-dump twin of the assembly-validation deck (one rank, so the
# device value refresh engages): the assembled sparse rows of the direct
# resistive block are written at the second assembly as the binary CSR
# file the offline solver benchmark reads, and the analysis checks the
# file against the operator's known structure -- one unknown per face,
# the axis/boundary identity rows, the decoupling of the poloidal and
# azimuthal components, and the exact symmetry of the row-scaled operator
# under the cylindrical face-volume weights (the property a Cholesky
# backend relies on).
implicit_mhd.resistive_direct_dump_prefix = diags/resistive_direct
implicit_mhd.resistive_direct_dump_assembly = 2
# the cuDSS option list in its ParmParse form (one quoted name=value token
# each): parsed and validated at Define on every build, applied only where
# cuDSS factorizes (the CPU path ignores it after validation)
implicit_mhd.resistive_direct_cudss_options = "pivot_type=none" "nd_nlevels=16"
