# base input parameters
FILE = inputs_test_rz_theta_implicit_mhd_resistive_pc_assembly_reduced

# Split twin (one rank): the kept rows are partitioned into the connected
# components of their coupling pattern -- the poloidal (B_r, B_z) block of
# 992 rows and the azimuthal (B_theta) block of 512 rows on this deck --
# and each is factorized and solved as its own system; the dumps of both
# must be disjoint, cover the kept rows, be exact restrictions of the full
# matrix and carry no coupling to each other.
implicit_mhd.resistive_direct_split_components = 1
